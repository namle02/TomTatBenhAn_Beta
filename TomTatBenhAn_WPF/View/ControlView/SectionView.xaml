<UserControl x:Class="TomTatBenhAn_WPF.View.ControlView.SectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TomTatBenhAn_WPF.View.ControlView"
             xmlns:dto="clr-namespace:TomTatBenhAn_WPF.Repos.Dto"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <StackPanel Margin="0,5">
        <!-- Tiêu đề section -->
        <Border Background="#E3F2FD" CornerRadius="3" Padding="10,5" Margin="0,2">
            <TextBlock
                FontSize="16"
                FontWeight="SemiBold"
                Foreground="#1976D2"
                Text="{Binding Title}"
                TextWrapping="Wrap" />
        </Border>

        <!-- Nội dung section -->
        <ItemsControl ItemsSource="{Binding Content}" Margin="15,5,0,5">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock
                        Margin="0,2"
                        FontSize="14"
                        Text="{Binding}"
                        TextWrapping="Wrap"
                        Foreground="#333" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Sub-sections (đệ quy) -->
        <ItemsControl ItemsSource="{Binding Children}" Margin="20,5,0,5">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type dto:SectionDTO}">
                    <local:SectionView DataContext="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
