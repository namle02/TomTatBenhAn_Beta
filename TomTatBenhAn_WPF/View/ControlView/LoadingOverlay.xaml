<UserControl
    x:Class="TomTatBenhAn_WPF.View.ControlView.LoadingOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <UserControl.Resources>
        <!--  Loading Animation  -->
        <Storyboard x:Key="LoadingAnimation" RepeatBehavior="Forever">
            <DoubleAnimation
                AutoReverse="False"
                Storyboard.TargetName="LoadingSpinner"
                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                From="0"
                To="360"
                Duration="0:0:1.5" />
        </Storyboard>

        <!--  Fade In Animation  -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                From="0"
                To="1"
                Duration="0:0:0.3" />
        </Storyboard>

        <!--  Fade Out Animation  -->
        <Storyboard x:Key="FadeOutAnimation">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                From="1"
                To="0"
                Duration="0:0:0.3" />
        </Storyboard>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}" />
            <BeginStoryboard Storyboard="{StaticResource LoadingAnimation}" />
        </EventTrigger>
    </UserControl.Triggers>

    <!--  Loading Overlay Background  -->
    <Grid Background="#80000000" Opacity="0">
        <Grid.Style>
            <Style TargetType="Grid">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsVisible, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}" />
                        </DataTrigger.EnterActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>

        <!--  Loading Card  -->
        <materialDesign:Card
            Padding="40,30"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="White">

            <StackPanel HorizontalAlignment="Center" Orientation="Vertical">

                <!--  Loading Spinner  -->
                <Grid
                    Width="60"
                    Height="60"
                    Margin="0,0,0,20">
                    <Ellipse
                        x:Name="LoadingSpinner"
                        Width="60"
                        Height="60"
                        RenderTransformOrigin="0.5,0.5"
                        Stroke="{DynamicResource PrimaryHueMidBrush}"
                        StrokeDashArray="15,25"
                        StrokeThickness="4">
                        <Ellipse.RenderTransform>
                            <RotateTransform Angle="0" />
                        </Ellipse.RenderTransform>
                    </Ellipse>

                    <!--  AI Icon  -->
                    <materialDesign:PackIcon
                        Width="30"
                        Height="30"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource PrimaryHueMidBrush}"
                        Kind="Brain" />
                </Grid>

                <!--  Loading Text  -->
                <TextBlock
                    Margin="0,0,0,10"
                    HorizontalAlignment="Center"
                    FontSize="18"
                    FontWeight="Medium"
                    Foreground="{DynamicResource MaterialDesignBody}"
                    Text="{Binding LoadingText, RelativeSource={RelativeSource AncestorType=UserControl}}" />

                <!--  Subtitle  -->
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="14"
                    Foreground="{DynamicResource MaterialDesignBodyLight}"
                    Opacity="0.7"
                    Text="Đang xử lý với AI ..." />

                <ProgressBar
                    Height="15"
                    Margin="0,10,0,0"
                    IsIndeterminate="True" />

                <!--  Progress Dots Animation  -->
                <StackPanel
                    Margin="0,15,0,0"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
                    <Ellipse
                        Width="8"
                        Height="8"
                        Margin="2"
                        Fill="{DynamicResource PrimaryHueMidBrush}">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Style.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation
                                                    AutoReverse="True"
                                                    Storyboard.TargetProperty="Opacity"
                                                    From="0.3"
                                                    To="1"
                                                    Duration="0:0:0.6" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>

                    <Ellipse
                        Width="8"
                        Height="8"
                        Margin="2"
                        Fill="{DynamicResource PrimaryHueMidBrush}">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Style.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation
                                                    AutoReverse="True"
                                                    BeginTime="0:0:0.2"
                                                    Storyboard.TargetProperty="Opacity"
                                                    From="0.3"
                                                    To="1"
                                                    Duration="0:0:0.6" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>

                    <Ellipse
                        Width="8"
                        Height="8"
                        Margin="2"
                        Fill="{DynamicResource PrimaryHueMidBrush}">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Style.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation
                                                    AutoReverse="True"
                                                    BeginTime="0:0:0.4"
                                                    Storyboard.TargetProperty="Opacity"
                                                    From="0.3"
                                                    To="1"
                                                    Duration="0:0:0.6" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </StackPanel>

            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
