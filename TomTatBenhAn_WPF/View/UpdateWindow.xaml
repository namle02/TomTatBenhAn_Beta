<Window x:Class="TomTatBenhAn_WPF.View.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:page="clr-namespace:TomTatBenhAn_WPF.View.PageView"
        Title="Cập nhật phần mềm"
        Width="600"
        Height="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}">
        <materialDesign:Card Padding="0" Margin="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header with close button -->
                <Grid Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" 
                             Text="Cập nhật phần mềm" 
                             FontSize="18" 
                             FontWeight="Bold"
                             Foreground="White"
                             VerticalAlignment="Center"
                             Margin="20,15"/>
                    <Button Grid.Column="1"
                          Width="40"
                          Height="40"
                          Style="{StaticResource MaterialDesignIconButton}"
                          Click="CloseButton_Click">
                        <materialDesign:PackIcon Kind="Close" 
                                               Foreground="White"/>
                    </Button>
                </Grid>

                <!-- Content -->
                <page:UpdatePage Grid.Row="1" DataContext="{Binding}"/>
            </Grid>
        </materialDesign:Card>
    </Border>
</Window>

